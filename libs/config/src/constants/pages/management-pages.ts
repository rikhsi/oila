export enum MANAGEMENT_ROUTE {
  settings = 'settings',
  options = 'options',
  category = 'category',
}

export enum MG_SETTINGS_ROUTE {
  translation = 'translation',
  users = 'users',
}

export enum UsersPages {
  userDetails = 'user-details',
  create = 'create',
  edit = 'edit',
  userId = 'userId',
}

export enum OPTION_PAGE {
  create = 'create',
  edit = 'edit',
  optionId = 'optionId',
}

export enum CATEGORY_PAGE {
  create = 'create',
  edit = 'edit',
  categoryId = 'categoryId',
}